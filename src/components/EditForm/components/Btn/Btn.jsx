import React from "react";
import styles from './Btn.module.css';

function Btn(props) {
    return (
        <button className={styles.btnEditForm}>accept changes</button>
    )
}

export default Btn;